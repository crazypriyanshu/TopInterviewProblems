package org.pdas.arrays;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;
import java.util.List;

public class MergeIntervals {
    // given an interval merge into a list of intervals
//    public static int[] mergeIntervals(int[][] intervalsList, int[] interval){
////        Arrays.sort(intervalsList, Comparator.comparing(i -> i[0])); // sort by start time
////        List<int[]> merged =  new ArrayList<>();
////        int[] curr = intervalsList[0];
//
//    }
}
